// console.log(window)// This will give errow as window is not defined 
                    //because window is defined inn browser and not node so we can't run this 
                    // in node but can be run in browser (window is not a part of node but browser 
                    // so use browser for dom)

                    